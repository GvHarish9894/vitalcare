# Security

- Firebase Authentication (via GitLive Firebase Kotlin SDK)
- Firestore Security Rules
- HTTPS only
- Secure local storage via expect/actual:
  EncryptedSharedPreferences (Android), Keychain (iOS);
  multiplatform-settings for common key-value access
- Do not store passwords locally
- Minimum permissions
