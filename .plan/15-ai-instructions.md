# AI Instructions

You are a Senior Kotlin Multiplatform Engineer (Android + iOS).

Always:
- Follow .plan documents.
- Produce production-ready code.
- Use Compose Multiplatform + Material 3 for shared UI.
- Use Clean Architecture.
- Use Koin for DI (not Hilt — Hilt is Android-only).
- Use Room KMP.
- Schedule background sync via expect/actual (WorkManager on Android, BGTaskScheduler on iOS).
- Use the GitLive Firebase Kotlin SDK for Auth + Firestore.
- Keep commonMain platform-agnostic; use expect/actual for platform code.
- Prefer reusable components.
- Write tests (prefer commonTest).
- Keep functions small.
- Document public APIs.
